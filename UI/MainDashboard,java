package ui;

import engine.StationEngine;
import model.ColonyTask;
import persistence.SaveLoadService;
import resources.Resource;
import resources.ResourceManager;

import javax.swing.*;
import java.awt.*;

public class MainDashboard extends JFrame {
    private StationEngine engine;
    private JTextArea logArea;
    private DefaultListModel<String> queueModel;
    private JLabel partsLabel, oxygenLabel, rationsLabel, creditsLabel;
    private JComboBox<Resource> supplyDropdown;

    public MainDashboard(StationEngine engine) {
        this.engine = engine;
        setTitle("Ares Base Command - Team 32");
        setSize(900, 600);
        setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
        setLayout(new BorderLayout(10, 10));
    }
}engine.setCallbacks(
    message -> logMessage(message),
    v -> refreshUI()
);

buildUI();
refreshUI();
engine.startEngine();JPanel resourcePanel = new JPanel(new FlowLayout(FlowLayout.CENTER, 20, 10));
resourcePanel.setBackground(Color.DARK_GRAY);

partsLabel = createStyledLabel("Parts: 0");
oxygenLabel = createStyledLabel("Oxygen: 0");
rationsLabel = createStyledLabel("Rations: 0");
creditsLabel = createStyledLabel("Credits: 0");
creditsLabel.setForeground(Color.YELLOW);

resourcePanel.add(partsLabel);
resourcePanel.add(oxygenLabel);
resourcePanel.add(rationsLabel);
resourcePanel.add(creditsLabel);
add(resourcePanel, BorderLayout.NORTH);

logArea = new JTextArea();
logArea.setEditable(false);
logArea.setBackground(Color.BLACK);
logArea.setForeground(Color.GREEN);
logArea.setFont(new Font("Monospaced", Font.PLAIN, 14));
JScrollPane logScroll = new JScrollPane(logArea);
logScroll.setBorder(BorderFactory.createTitledBorder("System Log"));
add(logScroll, BorderLayout.CENTER);